\NeedsTeXFormat{LaTeX2e}[1999/01/01]
\ProvidesPackage{mymacro}[2011/04/29]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Declare package options %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Language options
\newif\if@myenglish \@myenglishfalse
\DeclareOption{myenglish}{\@myenglishtrue}
\newif\if@myfrancais \@myfrancaisfalse
\DeclareOption{myfrancais}{\@myfrancaistrue}
% In case of unknown options
\DeclareOption*{%
	\PackageWarning{mymacro}{Unknown option `\CurrentOption'}%
}

\ProcessOptions

%% Options to pass to packages

%% Packages to call
\RequirePackage{xspace}
% Only for number printing
\RequirePackage{numprint}

%%%%%%%%%%%%%%%%%%%%%%%
%% New configuration %%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%
%% New commands %%
%%%%%%%%%%%%%%%%%%
% A master command on '\emph'
\newcommand{\myemph}[1]{\emph{#1}}
% To write a name
% The optionnal argument is for prefix on names (like 'van der' in 'van der Walls')
\newcommand{\myname}[2][]{%
	\def\my@arg@one{#1}%
	\if@myfrancais%
		\textsc{%
			\ifx\my@arg@one\@empty%
			\else%
				\MakeLowercase{#1}\xspace%
			\fi%
			\makefirstuc{\MakeLowercase{#2}}\xspace%
		}%
	\else%
	\ifx\my@arg@one\@empty%
	\else%
		\MakeLowercase{#1}\xspace%
	\fi%
	\MakeUppercase{#2}\xspace%
	\fi%
}
% To print numbers (see 'numprint' package)
\newcommand{\mynum}[1]{\nombre{#1}}

%% Symboles
% © : Copyright
\newcommand{\mycopyright}{\copyright}
% ® : Registered
\newcommand{\myregistered}{\textsuperscript{\textregistered}}
% ™ : Trade mark
\newcommand{\mytrademark}{\texttrademark}

%% Latin citations
% confer : se référer à
\newcommand{\mycf}{\textit{c.f.}\xspace}
% exempli gratia : par l'exemple (littéralement par la grâce de l'exemple)
\newcommand{\myeg}{\textit{e.g.}\xspace}
% et alii : et les autres
\newcommand{\myetal}{\textit{et al.}\xspace}
% et cetera desunt : et les autres choses manquent
\newcommand{\myetc}{\textit{etc.}\xspace}
% id est : c'est-à-dire
\newcommand{\myie}{\textit{i.e.}\xspace}

%% Mathematical operators
% To have a norm
\newcommand{\mynorm}[1]{\left\| #1 \right\|}

% End of package
\endinput
